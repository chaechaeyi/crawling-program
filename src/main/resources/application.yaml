debug: false
management.endpoints.web.exposure.include: "*"

logging:
  level:
    com.kia.assignment: debug
    org.springframework.web.servlet: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

spring:
  cache:
    ehcache:
      config: classpath:ehcache.xml
